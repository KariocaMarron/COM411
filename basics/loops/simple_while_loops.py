# Declare a variable to count the current numbered of iterations
iterations = 0

# Display ready message
print("Starting iterations")

# Display the current count
while iterations < 10:

    # Update the value of iterations count
    iterations = iterations + 1

# Display complation message
print(f"Completed {iterations}!")
