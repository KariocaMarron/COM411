import matplotlib.pyplot as plt # Importing the library


def display(x, y): # calling the definition
    x = [] # list of x values to be used
    y = [] # list of y values to be used
    plt.plot(x, y) # to plot the x and y values
    plt.show() # to present the result based in the imported library "plt" (matplotlib.pyplot)


plt.plot(x, y)
plt.show()
